# Sightline ML

Scoring models and the pipeline that combines them into recap clips.

TODO: lay out `models/` (one folder per model), `pipeline/` (score
aggregation + clip merging), and `evaluation/` (shared benchmark set).

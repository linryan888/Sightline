# Sightline Backend

API + worker: accepts uploaded footage, runs ML scoring, selects clips,
assembles recaps.

TODO: flesh out `main.py` into a proper FastAPI app package (routes, models,
schemas, services, worker).
